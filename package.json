{
  "name": "digital-badge",
  "version": "0.1.0",
  "description": "A simple dApp for managing digital badges",
  "dependencies": {
    "ethers": "^6.7.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  },
  "scripts": {
    "build": "esbuild ./src/index.js --bundle --outfile=dist/index.js --platform=node --target=node20",
    "start": "ROLLUP_HTTP_SERVER_URL=\"http://127.0.0.1:5004\" node src/index.js"
  },
  "keywords": [
    "cartesi",
    "badge",
    "achievement"
  ],
  "license": "MIT"
}
